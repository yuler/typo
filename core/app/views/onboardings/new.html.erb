<div class="bg-surface p-6 rounded-xl border shadow-sm w-full max-w-sm text-center">
  <h1 class="text-xl font-bold mb-2">Create your account</h1>
  <p class="text-muted text-sm mb-6">Complete your profile to get started.</p>

  <%= form_with model: @signup, url: onboarding_path, class: "flex flex-col gap-4 text-left" do |form| %>
    <% if @signup.errors.any? %>
      <div id="error_explanation" class="p-3 bg-red-50 text-red-500 text-sm rounded-md mb-2">
        <ul class="list-none">
          <% @signup.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group text-left">
      <%= form.label :username, class: "label" %>
      <%= form.text_field :username, required: true, autofocus: true, autocomplete: "username", 
          class: "input", placeholder: "your-username" %>
      <p class="text-xs text-muted mt-1"><%= request.host_with_port %>/<%= @signup.username.presence || "username" %></p>
    </div>

    <div class="form-group text-left">
      <%= form.label :nickname, class: "label" %>
      <%= form.text_field :nickname, required: true, placeholder: "Display name",
          class: "input" %>
      <p class="text-xs text-muted mt-1">This is how others will see you</p>
    </div>

    <%= form.submit "Create Account", class: "btn btn-primary mt-2" %>
  <% end %>
</div>
